import React from 'react'

type Props = {
    widgetMenu:string[];
    icons:any
}

const WidgetBar = (props: Props) => {
    const numWidgets=props.widgetMenu.length;
    let arr=[""]
  return (
    <div>
        {props && <>
        
        </>}
    </div>
  )
}

export default WidgetBar